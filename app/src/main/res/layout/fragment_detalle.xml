<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_main_unibell"
>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:layout_weight="1"
   >

    <ListView
        android:id="@+id/fp_lvuno"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="@dimen/pad_20dp"
        />

</RelativeLayout>

    <LinearLayout
        android:layout_alignParentBottom="true"
        android:id="@+id/lyTotalesLiquidacion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/label_blanco_login_unibell"
        android:orientation="horizontal"
        android:padding="16dp"
        >

    <TextView
        android:id="@+id/labelCobranzaTotal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:fontFamily="@font/sf_pro_display_bold"
        android:text="Cobranza Total"
        android:textColor="@color/Button_login_unibell"
        android:layout_gravity="bottom"
        android:textSize="16dp"
        android:layout_marginRight="10dp"
        />


    <TextView
        android:id="@+id/txtCobranzaTotal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:fontFamily="@font/sf_pro_display_light"
        android:text="S/ 1400.00"
        android:gravity="right"
        android:layout_weight="1"
        android:textColor="@color/label_color_menu"
        android:textSize="16dp" />

</LinearLayout>

</LinearLayout>